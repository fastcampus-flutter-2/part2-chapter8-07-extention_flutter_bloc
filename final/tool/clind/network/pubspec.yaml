name: tool_clind_network
description: tool clind network package.
publish_to: none

environment:
  sdk: 3.8.1
  flutter: 3.32.8

dependencies:
  flutter:
    sdk: flutter

  core_network:
    path: ../../../core/network